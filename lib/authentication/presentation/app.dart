/*
import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
import 'package:griot_app/authentication/presentation/blocs/authentication/auth_bloc.dart';
import 'package:griot_app/authentication/presentation/blocs/auth_state.dart';
import 'package:griot_app/authentication/presentation/pages/login_page.dart';

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Griot App',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      builder: (context, state) {
        if (state is UserLoggedInState) {
          return Container(); // TODO: replace with the page to show after successful login
        }
        // Show login page by default
        return LoginPage();
      },
    );
  }
}
*/
